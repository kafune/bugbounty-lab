requests      # bin/h1sync.py + bin/discover.py — HTTP (API H1 / bounty-targets-data)
pyjwt         # postman-auth.py --mint — assinatura RS256/HS256
cryptography  # backend de chave RSA para o pyjwt
# discover.py/score.py/state.py só usam requests + stdlib — sem deps novas.
